{
  "name": "primerelay",
  "version": "1.0.0",
  "description": "Professional SMS SaaS Platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend", 
    "worker"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=worker\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend && npm run build --workspace=worker",
    "install:all": "npm install --workspaces",
    "clean": "npm run clean --workspace=frontend && npm run clean --workspace=backend && npm run clean --workspace=worker"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
