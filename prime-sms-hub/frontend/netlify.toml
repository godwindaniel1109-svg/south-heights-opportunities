# Netlify Configuration for Frontend
[build]
  publish = "frontend"
  command = "echo 'Frontend static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
