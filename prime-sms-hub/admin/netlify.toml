# Netlify Configuration for Admin Panel
[build]
  publish = "admin"
  command = "echo 'Admin static site - no build needed'"

[[redirects]]
  from = "/*"
  to = "/admin-login.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
